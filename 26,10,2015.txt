int _tmain(int argc, _TCHAR* argv[]) - для чего нужны паармтеры( с командной строки параметры в программу)
argc = int (кол-во параметров, передаваемых в функцию(по дефолту = 1, так как первы йпараметр это имя программы(лиюо имя функции, либо программы, зависит от ОС))
argv - массив указаелей на строки(char) массив данных строкого типа
ПРИМЕР:
int _tmain(int argc, _TCHAR* argv[])
{if(argc>1){cout<<arv[1]<<"\n"}else{cout<<"NO ARGS";}



шаблон функции
int func(int i)
{cout<<i;}
int func(float i);
{cout<<i;}
int func(char i);
{cout<<i}
template - универсальный тип данных
template <typename T> - тимплейт ТЭ 
int func(T i)
{cout<<i} какой бы параметр сюда не передали, они будут выведены правильно и корректно!

Создать структуры данных стек
создать класс описывающий эту структуру
